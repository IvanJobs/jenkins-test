pipeline {
    agent any

    stages {
        stage('init') {
            steps {
                sh 'pwd'
                sh 'ls -lh'
                sh 'whoami'
                sh 'which npm'
                sh 'which node'
            }
        }
    }
}